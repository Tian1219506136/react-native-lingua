import { TabPlaceholderScreen } from "@/components/tab-placeholder-screen";

export default function AiTeacherScreen() {
  return (
    <TabPlaceholderScreen
      subtitle="Coming with the Stream + Vision Agent chapters. For now, start an audio session from Learn → any lesson."
      title="AI Teacher"
    />
  );
}
